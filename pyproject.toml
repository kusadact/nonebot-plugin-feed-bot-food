[project]
name = "nonebot-plugin-feed-bot-food"
version = "0.1.0"
description = "A NoneBot plugin for feeding the bot food."
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"
