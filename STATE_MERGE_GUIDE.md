# nekobot 两个 QQ 号合并投喂数据

这份文档根据 `cn194168` 上正在运行的 `nekobot` 实例和实际 `state.json` 编写。这里只提供合并指导，不直接修改服务器数据。

## 实例位置

- 容器：`nekobot`
- 宿主机数据文件：`/opt/nekobot-stack/nekobot/data/nonebot_plugin_feed_bot_food/state.json`
- 容器内数据文件：`/opt/nekobot/data/nonebot_plugin_feed_bot_food/state.json`
- 宿主机配置文件：`/opt/nekobot-stack/nekobot/.env`
- 当前状态格式：schema v3

当前 `bots` 中有三个键：

```text
2059765652
3336557690
probe-bot
```

本次只合并前两个真实 QQ 号，不要把 `probe-bot` 算进去，也不要删除它，确认合并结果正常后再清理。

## 已读取到的实际数据

服务器当前日期按 Asia/Shanghai 为 `2026-08-17`，插件的“今日”键也是 `2026-08-17`；“昨日”键是 `2026-08-16`。

| 数据 | `3336557690` | `2059765652` |
| --- | ---: | ---: |
| JSON 原始 `current_weight` | 56.45 kg | 41.49 kg |
| 今日投喂次数 | 5 | 12 |
| 今日累计摄入 | 3.59 kg | 5.71 kg |
| 昨日投喂次数 | 5 | 2 |
| 昨日累计摄入 | 3.59 kg | 1.68 kg |
| 昨日体重变化 | 0.00 kg | -0.43 kg |
| 历史投喂次数 | 194 | 390 |
| 历史累计摄入 | 110.81 kg | 185.61 kg |

## 按需求得到的目标值

你的取值规则是：

- 当前体重取 `3336557690`；
- 今日投喂次数、今日累计摄入取 `2059765652`；
- 昨日投喂次数、昨日体重变化取 `2059765652`；
- 历史投喂次数和历史累计摄入取两个账号相加。

历史相加结果为：

```text
历史投喂次数 = 194 + 390 = 584
历史累计摄入 = 110.81 + 185.61 = 296.42 kg
```

### 当前体重要注意 JSON 原始值和展示值的区别

插件的 `/查看体重` 展示值不是直接读取 JSON 的 `current_weight`，而是：

```text
展示当前体重 = JSON current_weight - 今日累计摄入
```

因此 `3336557690` 当前展示体重是：

```text
56.45 - 3.59 = 52.86 kg = 105.72 斤
```

如果你的意思是“合并后 `/查看体重` 仍显示 `3336557690` 当前体重”，而今日累计摄入又要改取 `2059765652` 的 `5.71 kg`，共享状态中的原始 `current_weight` 应写成：

```text
52.86 + 5.71 = 58.57 kg
```

这样插件展示的结果才是 `58.57 - 5.71 = 52.86 kg`。如果直接把原始值 `56.45` 搬过去，最终展示会变成 `50.74 kg`，不是原账号看到的当前体重。

## 当前数据结构下的一个冲突

插件没有单独保存“历史累计摄入”字段，状态查询时会把 `daily[*].gain` 全部相加得到历史累计摄入。因此下面两个要求不能同时无损满足：

1. 今日 `gain` 必须只取 `2059765652` 的 `5.71 kg`；
2. 历史累计摄入必须严格等于两个账号原始历史之和 `296.42 kg`。

如果按第 1 条覆盖今日 `gain`，历史累计摄入会少掉 `3336557690` 今日的 `3.59 kg`，查询结果为：

```text
296.42 - 3.59 = 292.83 kg = 585.66 斤
```

如果优先保证历史累计摄入严格为 `296.42 kg`，今日 `gain` 就必须保存两个账号相加的 `9.30 kg`，不再是你指定的 `5.71 kg`。

因此正式合并前需要在这两个口径中选一个；如果两个数都必须准确，需要先改插件，让它单独持久化历史累计摄入，再进行迁移。

昨日累计摄入你没有单独指定。若按“历史摄入相加”处理，昨日 `gain` 为 `3.59 + 1.68 = 5.27 kg`；昨日投喂次数和昨日体重变化仍按你的要求取 `2059765652` 的 `2` 和 `-0.43 kg`。

## 实际操作顺序

不要先打开共享配置再合并数据，否则插件会先创建一份初始的 `__shared__` 状态。

1. 停止容器，确保没有新的投喂写入：

   ```bash
   docker stop nekobot
   ```

2. 备份状态文件：

   ```bash
   cp /opt/nekobot-stack/nekobot/data/nonebot_plugin_feed_bot_food/state.json \
      /opt/nekobot-stack/nekobot/data/nonebot_plugin_feed_bot_food/state.json.before-merge-20260817
   ```

3. 基于上面的口径生成 `bots["__shared__"]`：

   - `initial_weight` 取 `48.00`；
   - `current_weight`：若目标是保持 `/查看体重` 的展示值，取 `58.57`；
   - 今日 `daily["2026-08-17"].feed_count` 和 `gain` 取 `2059765652` 的 `12`、`5.71`；
   - 昨日 `daily["2026-08-16"].feed_count` 和 `weight_change` 取 `2059765652` 的 `2`、`-0.43`；
   - `total_feed_count` 取 `584`；
   - `events` 合并两个账号的事件，当前两边分别有 34 和 14 条，合并后为 48 条；
   - `last_settled_date` 设为 `2026-08-16`，避免重启后再次重算你指定的昨日体重变化；
   - 保留原来的两个 QQ 号和 `probe-bot` 分区，先不要删除。

4. 在宿主机 `/opt/nekobot-stack/nekobot/.env` 的 feed-bot-food 配置段加入：

   ```dotenv
   FEED_BOT_FOOD__SHARED_STATE=true
   ```

5. 启动容器：

   ```bash
   docker start nekobot
   ```

   容器启动命令会执行 `uv sync`，并从 GitHub 的 `master` 安装插件当前版本。

## 验证结果

启动后在群里执行 `/查看体重`，至少检查：

- 当前体重是否为 `105.72 斤`（如果按展示值口径合并）；
- 今日投喂次数是否为 `12`；
- 今日累计摄入是否为 `11.42 斤`；
- 昨日投喂次数是否为 `2`；
- 昨日体重变化是否为 `-0.86 斤`；
- 历史投喂次数是否为 `584`；
- 历史累计摄入按所选口径应为 `585.66 斤` 或 `592.84 斤`。

确认无误后，再用两个 QQ 号分别投喂一次并查询状态，确认它们都写入 `bots["__shared__"]`。出现异常时先停止容器，用备份文件恢复，再处理配置。
